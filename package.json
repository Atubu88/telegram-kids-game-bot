{
  "name": "telegram-kids-game-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute kids_game_bot --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute kids_game_bot --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}
